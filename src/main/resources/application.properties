# Server Configuration
server.port=8080
server.address=0.0.0.0

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/name_rotation_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=NIYONSHUTI@!alpha@250
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.check-template-location=false

# Logging
logging.level.org.hibernate.SQL=DEBUG

# Email Configuration (Gmail example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Your Gmail address
spring.mail.username=niyonshutialpha7@gmail.com

# The App Password (16 characters without spaces)
spring.mail.password=kvrmjvxczmykdrxt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# For other email providers:
# Outlook: smtp.office365.com, port 587
# Yahoo: smtp.mail.yahoo.com, port 587
# Custom SMTP: your-smtp-server.com, port 587